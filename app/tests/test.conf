[User]
name=test user
email=test@user.co

[Build]
host=localhost
base_path=~/tmp_herd_test_base_build_path

[Security]
gnupg_home=app/tests/gnupghome
remote_secret_store=sec.iadops.com

# hash algorithm option for verifying the contents of a secret file
# this should be the name of the hash function from the PyCrypto library
# hash_algo=SHA256